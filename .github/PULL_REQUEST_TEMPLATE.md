## What changes

<!-- One paragraph. User-visible behaviour first, then internals. -->

## Verification

<!-- typecheck / lint / test counts / deno check / migration dry-run, as applicable -->

## After merge

<!-- Migrations to apply (in order), Edge Functions to redeploy (--no-verify-jwt), env vars or secrets, then `vercel deploy --prod`. Write "none" if nothing. -->
